---
version: alpha
name: Modal jazz · the cool-blue session
description: A modal-jazz mood of deep blue, midnight teal, brass, and smoke
colors:
  neutral-background: "oklch(0.9559 0.0011 17.18)"
  neutral-background-dark: "oklch(0.2343 0.0038 106.69)"
  neutral-surface: "oklch(0.9345 0.0017 67.8)"
  neutral-surface-dark: "oklch(0.2606 0.004 84.58)"
  neutral-surface-high: "oklch(0.913 0.0029 84.56)"
  neutral-surface-high-dark: "oklch(0.2853 0.0039 84.58)"
  neutral-on-surface: "oklch(0.1772 0.002 106.6)"
  neutral-on-surface-dark: "oklch(1 0 89.88)"
  neutral-on-surface-variant: "oklch(0.3794 0.0088 88.73)"
  neutral-on-surface-variant-dark: "oklch(0.8236 0.0073 88.65)"
  neutral-outline-variant: "oklch(0.6035 0.017 90.31 / 30%)"
  neutral-outline-variant-dark: "oklch(0.6035 0.017 90.31 / 30%)"
  neutral: "oklch(0.5586 0.0155 93.1)"
  neutral-dark: "oklch(0.6469 0.0167 90.3)"
  neutral-hover: "oklch(0.4705 0.0128 95.33)"
  neutral-hover-dark: "oklch(0.7368 0.0143 97.51)"
  neutral-active: "oklch(0.3794 0.0088 88.73)"
  neutral-active-dark: "oklch(0.8236 0.0073 88.65)"
  neutral-disabled: "oklch(0.6035 0.017 90.31 / 60%)"
  neutral-disabled-dark: "oklch(0.6035 0.017 90.31 / 60%)"
  neutral-on-neutral: "oklch(1 0 89.88)"
  neutral-on-neutral-dark: "oklch(1 0 89.88)"
  primary: "oklch(0.5596 0.0962 255.73)"
  primary-dark: "oklch(0.6486 0.095 255.5)"
  primary-hover: "oklch(0.4695 0.0745 255.92)"
  primary-hover-dark: "oklch(0.7365 0.0701 256.11)"
  primary-active: "oklch(0.3781 0.0495 255.54)"
  primary-active-dark: "oklch(0.8241 0.0404 256.6)"
  primary-disabled: "oklch(0.6037 0.101 256.19 / 60%)"
  primary-disabled-dark: "oklch(0.6037 0.101 256.19 / 60%)"
  primary-on-primary: "oklch(1 0 89.88)"
  primary-on-primary-dark: "oklch(1 0 89.88)"
  primary-muted: "oklch(0.5595 0.053 211.01)"
  primary-muted-dark: "oklch(0.6468 0.0572 210.97)"
  primary-muted-hover: "oklch(0.4688 0.0418 211.55)"
  primary-muted-hover-dark: "oklch(0.7364 0.0515 210.69)"
  primary-muted-disabled: "oklch(0.6036 0.0571 209.61 / 60%)"
  primary-muted-disabled-dark: "oklch(0.6036 0.0571 209.61 / 60%)"
  primary-muted-on-primary-muted: "oklch(1 0 89.88)"
  primary-muted-on-primary-muted-dark: "oklch(1 0 89.88)"
  secondary: "oklch(0.5591 0.0675 80.26)"
  secondary-dark: "oklch(0.6479 0.0744 80.83)"
  secondary-hover: "oklch(0.4702 0.0526 81.27)"
  secondary-hover-dark: "oklch(0.7373 0.0666 80.49)"
  secondary-disabled: "oklch(0.6041 0.0733 81.04 / 60%)"
  secondary-disabled-dark: "oklch(0.6041 0.0733 81.04 / 60%)"
  secondary-on-secondary: "oklch(1 0 89.88)"
  secondary-on-secondary-dark: "oklch(1 0 89.88)"
  secondary-muted: "oklch(0.5582 0.018 233.27)"
  secondary-muted-dark: "oklch(0.6477 0.0193 229.17)"
  secondary-muted-hover: "oklch(0.4691 0.0149 236.91)"
  secondary-muted-hover-dark: "oklch(0.7368 0.0161 229.05)"
  secondary-muted-disabled: "oklch(0.6043 0.0196 229.21 / 60%)"
  secondary-muted-disabled-dark: "oklch(0.6043 0.0196 229.21 / 60%)"
  secondary-muted-on-secondary-muted: "oklch(1 0 89.88)"
  secondary-muted-on-secondary-muted-dark: "oklch(1 0 89.88)"
  accent: "oklch(0.5591 0.0605 77.85)"
  accent-dark: "oklch(0.6481 0.0663 78.21)"
  accent-hover: "oklch(0.471 0.0479 78.22)"
  accent-hover-dark: "oklch(0.7369 0.0577 78.4)"
  accent-disabled: "oklch(0.6021 0.0649 76.84 / 60%)"
  accent-disabled-dark: "oklch(0.6021 0.0649 76.84 / 60%)"
  accent-on-accent: "oklch(1 0 89.88)"
  accent-on-accent-dark: "oklch(1 0 89.88)"
  accent-muted: "oklch(0.5578 0.0475 56.14)"
  accent-muted-dark: "oklch(0.6478 0.0496 54.93)"
  accent-muted-hover: "oklch(0.4701 0.0364 57.23)"
  accent-muted-hover-dark: "oklch(0.7367 0.0398 56.73)"
  accent-muted-disabled: "oklch(0.6034 0.0503 56.95 / 60%)"
  accent-muted-disabled-dark: "oklch(0.6034 0.0503 56.95 / 60%)"
  accent-muted-on-accent-muted: "oklch(1 0 89.88)"
  accent-muted-on-accent-muted-dark: "oklch(1 0 89.88)"
  danger: "oklch(0.5588 0.2277 26.92)"
  danger-dark: "oklch(0.6481 0.2377 27.04)"
  danger-hover: "oklch(0.4693 0.1898 26.87)"
  danger-hover-dark: "oklch(0.7372 0.1606 27.05)"
  danger-disabled: "oklch(0.6045 0.2464 27.04 / 60%)"
  danger-disabled-dark: "oklch(0.6045 0.2464 27.04 / 60%)"
  danger-on-danger: "oklch(1 0 89.88)"
  danger-on-danger-dark: "oklch(1 0 89.88)"
  success: "oklch(0.5595 0.0931 152.27)"
  success-dark: "oklch(0.648 0.1057 151.89)"
  success-hover: "oklch(0.469 0.0704 152.37)"
  success-hover-dark: "oklch(0.7365 0.0992 152.24)"
  success-disabled: "oklch(0.6039 0.1033 151.92 / 60%)"
  success-disabled-dark: "oklch(0.6039 0.1033 151.92 / 60%)"
  success-on-success: "oklch(1 0 89.88)"
  success-on-success-dark: "oklch(1 0 89.88)"
  warning: "oklch(0.559 0.0967 71.98)"
  warning-dark: "oklch(0.648 0.109 72.47)"
  warning-hover: "oklch(0.4704 0.0737 73.01)"
  warning-hover-dark: "oklch(0.7359 0.1009 72)"
  warning-disabled: "oklch(0.6037 0.1059 72.21 / 60%)"
  warning-disabled-dark: "oklch(0.6037 0.1059 72.21 / 60%)"
  warning-on-warning: "oklch(1 0 89.88)"
  warning-on-warning-dark: "oklch(1 0 89.88)"
  info: "oklch(0.5596 0.1362 255.02)"
  info-dark: "oklch(0.6472 0.1357 255.05)"
  info-hover: "oklch(0.4704 0.1044 254.64)"
  info-hover-dark: "oklch(0.7373 0.0981 254.62)"
  info-disabled: "oklch(0.6028 0.1427 254.94 / 60%)"
  info-disabled-dark: "oklch(0.6028 0.1427 254.94 / 60%)"
  info-on-info: "oklch(1 0 89.88)"
  info-on-info-dark: "oklch(1 0 89.88)"
typography:
  display-sm:
    fontFamily: Trade Gothic Bold Condensed No. 20
    fontSize: 44px
    fontWeight: 700
    lineHeight: 0.977
  heading-lg:
    fontFamily: News Gothic
    fontSize: 32px
    fontWeight: 600
    lineHeight: 1.156
    letterSpacing: 0.0387em
  heading-md:
    fontFamily: News Gothic
    fontSize: 24px
    fontWeight: 600
    lineHeight: 1.167
    letterSpacing: 0.0417em
  heading-sm:
    fontFamily: News Gothic
    fontSize: 20px
    fontWeight: 600
    lineHeight: 1.15
    letterSpacing: 0.04em
  kicker-md:
    fontFamily: Courier Prime
    fontSize: 12px
    fontWeight: 520
    lineHeight: 1.417
    letterSpacing: 0.16em
  lead-md:
    fontFamily: New Caledonia
    fontSize: 18px
    fontWeight: 400
    lineHeight: 1.444
    letterSpacing: -0.005em
  body-md:
    fontFamily: New Caledonia
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.625
  body-sm:
    fontFamily: New Caledonia
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.615
  ui-md:
    fontFamily: Helvetica Neue
    fontSize: 12px
    fontWeight: 460
    lineHeight: 1.583
    letterSpacing: 0.01em
  ui-sm:
    fontFamily: Helvetica Neue
    fontSize: 11px
    fontWeight: 460
    lineHeight: 1.545
    letterSpacing: 0.01em
  caption-md:
    fontFamily: Helvetica Neue
    fontSize: 12px
    fontWeight: 440
    lineHeight: 1.5
  code-md:
    fontFamily: Courier Prime
    fontSize: 12px
    fontWeight: 460
    lineHeight: 1.5
spacing:
  none: 0px
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
  xl: 24px
  2xl: 32px
  3xl: 48px
  4xl: 64px
  5xl: 96px
rounded:
  none: 0px
  xs: 4px
  sm: 8px
  md: 12px
  lg: 16px
  xl: 28px
  full: 9999px
components:
  button-primary:
    backgroundColor: "{colors.primary}"
    textColor: "{colors.primary-on-primary}"
    typography: "{typography.ui-md}"
    rounded: "{rounded.md}"
    padding: 12px
  button-primary-hover:
    backgroundColor: "{colors.primary-hover}"
  button-primary-active:
    backgroundColor: "{colors.primary-active}"
  button-secondary:
    backgroundColor: "{colors.secondary}"
    textColor: "{colors.secondary-on-secondary}"
    typography: "{typography.ui-md}"
    rounded: "{rounded.md}"
    padding: 12px
  input:
    backgroundColor: "{colors.neutral-surface}"
    textColor: "{colors.neutral-on-surface}"
    typography: "{typography.body-md}"
    rounded: "{rounded.sm}"
    padding: 12px
  card:
    backgroundColor: "{colors.neutral-surface}"
    textColor: "{colors.neutral-on-surface}"
    rounded: "{rounded.lg}"
    padding: 24px
  chip:
    backgroundColor: "{colors.secondary-muted}"
    textColor: "{colors.secondary-muted-on-secondary-muted}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  tag-accent:
    backgroundColor: "{colors.accent}"
    textColor: "{colors.accent-on-accent}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.xs}"
    padding: 4px
  badge-primary-muted:
    backgroundColor: "{colors.primary-muted}"
    textColor: "{colors.primary-muted-on-primary-muted}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  badge-accent-muted:
    backgroundColor: "{colors.accent-muted}"
    textColor: "{colors.accent-muted-on-accent-muted}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  badge-danger:
    backgroundColor: "{colors.danger}"
    textColor: "{colors.danger-on-danger}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  badge-success:
    backgroundColor: "{colors.success}"
    textColor: "{colors.success-on-success}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  badge-warning:
    backgroundColor: "{colors.warning}"
    textColor: "{colors.warning-on-warning}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  badge-info:
    backgroundColor: "{colors.info}"
    textColor: "{colors.info-on-info}"
    typography: "{typography.ui-sm}"
    rounded: "{rounded.full}"
    padding: 4px
  popover:
    backgroundColor: "{colors.neutral-surface-high}"
    textColor: "{colors.neutral-on-surface}"
    rounded: "{rounded.lg}"
    padding: 16px
---

# Modal jazz · the cool-blue session — Design System

_Read this file as your instructions — it is the prompt. Token values are normative; the prose explains
how to apply them. Every color role ships a light value and a `-dark` sibling: pick the pair, not one
end. (Generated by Ultimate Tokens.)_

## Overview

Color from the modal session: midnight blue, teal shadow, brass gold, smoke grey, and a warm spotlight.

Restraint over decoration: whitespace, hierarchy, one decisive action per view.

Deliberately refused: Faded sepia. Modal jazz is deep blue and teal warmed by brass — a cool-blue mood, not a brown nostalgia.

## Colors

Reason over **roles**, never raw hexes. Each role names its light value; the frontmatter carries the
`-dark` sibling. Fill/on-pairs follow the kit's `onColorMode` setting; contrast is measured

and disclosed in the bundle receipt — values are the kit's own, never silently corrected.

### Token naming

Every color token follows the **Ultimate Tokens grammar**: `--{prefix}-{family}-{slot}`. This
project's prefix is `md-sys-color`; its families are `neutral`, `primary`, `primary-muted`, `secondary`, `secondary-muted`, `accent`, `accent-muted`, `danger`, `success`, `warning`, `info`. Construct names, do not invent them:

- The family name alone is the fill: `--md-sys-color-primary`, `--md-sys-color-danger`.
- Text/icons ON a family fill: `--md-sys-color-{family}-on-{family}`.
- States suffix the fill: `-hover`, `-active`, `-disabled`.
- App surfaces live in the neutral family: `--md-sys-color-neutral-background`, `-surface`, `-surface-high`; text on
  them: `-on-surface`, `-on-surface-variant`; hairlines: `-outline-variant`.
- **Prefix-adaptive**: under another prefix (`--md-sys-*`, `--color-*`), keep `{family}-{slot}` intact and swap only the prefix.

- **Surfaces** — the room, lowest to top: Background `{colors.neutral-background}` / Surface `{colors.neutral-surface}` /
  Surface-raised `{colors.neutral-surface-high}`. **Foreground `{colors.neutral-on-surface}`** — primary text; **Muted
  `{colors.neutral-on-surface-variant}`** — secondary text; **Border `{colors.neutral-outline-variant}`** — a translucent hairline (same value both schemes).
- **Primary `{colors.primary}`** — the one decisive action per view: CTAs, links, selection. `-hover`/`-active` carry its states. Its label is `{colors.primary-on-primary}`.
- **Secondary `{colors.secondary}`** — supporting actions and quieter emphasis. Its label is `{colors.secondary-on-secondary}`.
- **Accent `{colors.accent}`** — highlights, tags, and accents. Its label is `{colors.accent-on-accent}`.
- **Primary Muted `{colors.primary-muted}`** — a signature brand light — small, loud reads (featured/live markers), never a field of color. Its label is `{colors.primary-muted-on-primary-muted}`.
- **Accent Muted `{colors.accent-muted}`** — a signature brand light — small, loud reads (featured/live markers), never a field of color. Its label is `{colors.accent-muted-on-accent-muted}`.
- **Secondary Muted `{colors.secondary-muted}`** — a light metallic fill in both schemes with a near-black label: chips, tags, meta badges. Its label is `{colors.secondary-muted-on-secondary-muted}`.
- **Danger `{colors.danger}`** — status only — intent colors carry meaning, never decoration. Its label is `{colors.danger-on-danger}`.
- **Success `{colors.success}`** — status only — intent colors carry meaning, never decoration. Its label is `{colors.success-on-success}`.
- **Warning `{colors.warning}`** — status only — intent colors carry meaning, never decoration. Its label is `{colors.warning-on-warning}`.
- **Info `{colors.info}`** — status only — intent colors carry meaning, never decoration. Its label is `{colors.info-on-info}`.

**Pairing law.** Text on a family fill uses that family's `on-{family}` token — which differs by
scheme (light fills pair with white; the brighter dark-scheme fills pair with near-black). Text on
background/surface uses `on-surface` or `on-surface-variant`. A crossed pair fails contrast in one scheme.

## Typography

Set size **and** line-height **and** weight together from one level, never free-type; leading is a
unitless factor and any tracking is em — never px. The frontmatter carries the working scale
(12 levels). Weight is voice — never interchange the weights across roles.

Always include the **text-rendering baseline** in the app's global CSS — it is part of this design
system, not an option:

```css
html {
  -webkit-font-smoothing: antialiased;  /* macOS pair: consistent weight in light AND dark */
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;   /* kerning + ligatures engaged */
  font-optical-sizing: auto;            /* variable fonts use their optical axes */
  font-synthesis: none;                 /* no faux bold/italic — weights resolve from the font */
  font-kerning: normal;
  font-variant-ligatures: common-ligatures;
}
code, pre, kbd { font-variant-ligatures: none; } /* code-like units never ligate */
```

## Layout

Compose every gap and padding from the spacing scale (`{spacing.xs}` … `{spacing.5xl}`); an off-scale
gap does not exist. Keep a comfortable reading measure (~60–75ch), align to a consistent grid, and let
whitespace do the separating — borders are a last resort. Group related content in cards with generous
internal padding (`{spacing.xl}`).

## Elevation & Depth

Elevation is a **surface step, not a drop shadow**: `background` → `surface` → `surface-high`. A shadow
is optional garnish on the top-most surfaces (popovers, menus) — one soft low-alpha layer at most.
A modal/dialog's own backdrop is a separate, fixed system constant — `--md-sys-color-dialog-backdrop`
(opaque black at 80% alpha) — neutral chrome, never a brand-tinted scrim.

## Shapes

Soft-but-engineered: chips and tags `{rounded.xs}`, inputs `{rounded.sm}`, buttons `{rounded.md}`, cards
and panels `{rounded.lg}`, modals `{rounded.xl}`, pills and avatars `{rounded.full}`. One radius language
per view — rounded and sharp corners do not mix.

## Iconography

**Phosphor · regular** (MIT) is this system's icon set — `https://phosphoricons.com`.
Use the **regular** weight everywhere; mixing weights across one view reads as two systems.

Icon SIZES come from the control ramp, never from the glyph: xs 14px · sm 16px · md 18px · lg 20px · xl 24px · 2xl 28px. An icon centers in a square cell of side = its control's height, so it scales with the control, not on its own.

Do NOT substitute another icon set, and do NOT mix emoji into the icon layer — an icon carries meaning in the
system's own hand; an emoji imports someone else's.

## Motion

Motion is a token, never a number you type. Bind the curves and the ms ladder below; a raw
`300ms ease` in a component is a defect.

**Duration — tier by scope.** Small components (switch, checkbox) run short
(`100`–`200`ms); partial-screen surfaces (menu, drawer, card) run medium
(`250`–`400`ms); full-screen transitions run long (`450`ms+). **100ms is the "instant" floor** —
feedback faster than that is felt as immediate; past ~400ms a transition starts reading as slow.

**Entrances decelerate. Exits accelerate — and run faster.** The user is done with the thing and
waiting for what's next: a drawer opens in ~250ms and closes in ~200ms; a modal appears in ~300ms
and dismisses in ~200ms. Symmetric timing reads as sluggish dismissal. The asymmetry FLIPS with the
initiator: user-initiated UI answers fast and departs gently; system-initiated UI (an error) enters
slowly enough to be noticed and gets out of the way fast.

**Easing.** Entrances `cubic-bezier(0, 0, 0, 1)`; exits `cubic-bezier(0.3, 0, 1, 1)`; on-screen utility motion
`cubic-bezier(0.2, 0, 0, 1)`; technical motion (spinners, progress) stays `linear`. The **emphasized** family
is for hero moments only — everywhere-emphasized is just standard with extra cost.

**Animate `transform` and `opacity`, nothing else** — they are the only properties the compositor
holds at 60fps off the main thread. Never animate:

- **layout properties (`top`/`left`/`width`/`height`)** — they trigger layout + paint; animated layout shift is a CLS defect — animate `transform` instead.
- **anything but `transform`/`opacity`, by default** — only compositor properties hold 60fps off the main thread.
- **text while it is being read** — moving/blinking text impedes reading (WCAG 2.2.2 — the user must be able to pause it).
- **focus indicators, via reflow** — showing an outline must never shift layout (WCAG 2.4.11).
- **the text caret** — the blink is OS-defined; caret color interpolates discretely anyway.
- **form fields during entry** — state animation mid-typing is cognitive load — value changes land instantly.
- **decorative loops with no pause control** — WCAG 2.2.2's five-second rule.

**Reduced motion: reduce, don't remove.** Honour `@media (prefers-reduced-motion: reduce)` — it means
"this user gets vestibular-safe motion", not "this user wants a static page". Substitute a cross-fade
for the vestibular triggers (parallax, background video, zoom/scale, spin, slide-everything transitions),
keep the opacity change, and keep every state legible without motion. Nothing flashes more than three
times per second, ever.

## Components

State the interactive states explicitly — generic output betrays itself in hover/focus/disabled.

- **Buttons.** `button-primary` per the frontmatter; **hover** `{colors.primary-hover}`, **active**
  `{colors.primary-active}` (each ships both scheme ends); **focus** a 2px `{colors.primary}` outline at 2px
  offset; **disabled** `{colors.primary-disabled}` (the inert 60% wash, mode-independent). EVERY fill family
  carries its own `-hover` and `-disabled` — any intent is a real button; state fills and labels are the kit's role values under its `onColorMode` setting.
- **Inputs.** `{colors.neutral-surface}` field, 1px `{colors.neutral-outline-variant}`, `{colors.neutral-on-surface}` text,
  `{colors.neutral-on-surface-variant}` placeholder; **focus** swaps the border to `{colors.primary}` plus a 2px ring.
- **Cards.** `{colors.neutral-surface}` on `{colors.neutral-background}`, 1px `{colors.neutral-outline-variant}`, `{rounded.lg}`.
- **Badges / chips.** Intent or signature fill with its own `on-{family}`, pill radius. `secondary-muted` is the default metadata chip.

## Do's and Don'ts

**Three hard rules:**

- ❌ **Never hardcode a color.** Every color is a role — bind to the role so a re-theme flows everywhere.
- ❌ **Never cross an on-pair.** Use the fill's own `on-{family}` token (per scheme), or contrast fails in one scheme.
- ❌ **Never stack competing primaries.** One `primary` action per view.

**Prefer:**

- Reach for `danger`/`success`/`warning`/`info` only for status; `primary-muted`, `secondary-muted`, `accent-muted` are brand light, not status — small reads, never fields of color.
- Elevate by stepping the surface ladder, not by heavy shadows.
- Compose spacing, radii, and type from the scales; express states with the `-hover`/`-active`/`-disabled` tokens, not raw opacity guesses.
- Express motion with the easing + duration tokens (entrances decelerate, exits accelerate and run faster); never type a raw ms or `cubic-bezier()` into a component, and animate only `transform`/`opacity`.
- Let `secondary-muted` carry quiet metallic emphasis — small reads; keep `secondary` for actions.

## Responsive Behavior

Design mobile-first; columns stack below ~640px. Reduce display sizes on small screens but keep primary
reading text at `body-md` or larger; the smaller steps are for dense, secondary UI. Touch targets ≥ 44px.
Both schemes must hold at every width.

## Agent Prompt Guide
You are generating UI for **Modal jazz · the cool-blue session**. Work in this order:
1. **Tokens first** — colors, type, spacing, radii from the frontmatter; never invent a value. If a
   `tokens.json` arrived beside this file, it carries these same values plus the FULL extended layers
   (`semantic`/`semanticDark`, the complete `geometry` system) — prefer it for exhaustive lookups.
2. **Roles, then scheme** — pick the semantic role; both ends are provided, so never hand-roll a dark
   variant. Define the roles once as custom properties with native scheme switching — `color-scheme`
   on `:root` is required or the dark end never fires:
   ```css
   :root {
     color-scheme: light dark;
     --md-sys-color-primary: light-dark(oklch(0.5596 0.0962 255.73), oklch(0.6486 0.095 255.5));
     --md-sys-color-primary-on-primary: light-dark(oklch(1 0 89.88), oklch(1 0 89.88));
     /* …every role, from its light + -dark pair… */
   }
   ```
3. **Scale, then states** — size and space from the scales, then add hover/focus/active/disabled from
   the Components section — states are where generic output shows.
4. **One focus per view** — a single `primary` action; signature families are small reads; intent colors speak only for status.
5. **Name by grammar** — construct every token as `--md-sys-color-{family}-{slot}`; if the host carries a different prefix, adapt the prefix and keep `{family}-{slot}` intact.
When rules conflict, the three hard rules win. Mirror the structure and pairing of the `components/` previews.
